// Task 1 only provides the local configuration and Network client boundary.
// The MCP tool wiring arrives in a later task; do not start a partial service.
console.error("FRELY_MCP_NOT_READY");
process.exitCode = 1;
