# frely-mcp

Local stdio MCP client for the Frely static-provider acceptance profile. It resolves one configured vision Provider through a loopback Frely Network and keeps payment state in a local journal.

Real payment is disabled by default. Enabling it does not prove that the configured Hedera account exists, is funded, or has completed a settlement.
