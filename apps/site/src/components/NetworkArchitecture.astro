<section id="architecture" class="story-section architecture" aria-labelledby="architecture-heading" data-testid="network-architecture">
  <div class="architecture-layout">
    <div class="architecture-intro" data-reveal>
      <h2 id="architecture-heading" class="story-heading"><span>Three layers.</span><span>One open</span><span>network.</span></h2>
      <p>Built on Frely and Frely Swarm. Frely Network adds identity, discovery and paid access to existing AI services.</p>
    </div>

    <figure class="architecture-diagram">
      <figcaption class="sr-only">A verified request travels from Frely Network, through Frely, to Frely Swarm.</figcaption>
      <ol class="architecture-layers">
        <li>
          <div data-architecture-item>
          <div class="architecture-layer architecture-layer--network" data-architecture-card tabindex="0" role="group" aria-labelledby="architecture-network-title" aria-describedby="architecture-network-description">
            <span class="architecture-node" aria-hidden="true"><img src="/favicon.png" width="32" height="32" alt="" /></span>
            <div class="architecture-product"><span>Open network</span><h3 id="architecture-network-title">Frely Network</h3></div>
            <p id="architecture-network-description">Identity, discovery and payments.<span>Public, verifiable service records.</span></p>
          </div>
          </div>
          <div class="architecture-connector"><svg viewBox="0 0 16 46" fill="none" aria-hidden="true"><path d="M8 0v43m-6-6 6 6 6-6" stroke="currentColor" stroke-width="1.25" /></svg><span>Verified A2A request</span></div>
        </li>
        <li>
          <div data-architecture-item>
          <div class="architecture-layer" data-architecture-card tabindex="0" role="group" aria-labelledby="architecture-service-title" aria-describedby="architecture-service-description">
            <span class="architecture-node" aria-hidden="true">02</span>
            <div class="architecture-product"><span>Service access</span><h3 id="architecture-service-title">Frely</h3></div>
            <p id="architecture-service-description">User access, authentication and A2A routing.</p>
          </div>
          </div>
          <div class="architecture-connector"><svg viewBox="0 0 16 46" fill="none" aria-hidden="true"><path d="M8 0v43m-6-6 6 6 6-6" stroke="currentColor" stroke-width="1.25" /></svg><span>Task dispatch</span></div>
        </li>
        <li>
          <div data-architecture-item>
          <div class="architecture-layer" data-architecture-card tabindex="0" role="group" aria-labelledby="architecture-execution-title" aria-describedby="architecture-execution-description">
            <span class="architecture-node" aria-hidden="true">03</span>
            <div class="architecture-product"><span>Private execution</span><h3 id="architecture-execution-title">Frely Swarm</h3></div>
            <p id="architecture-execution-description">Hosts and runs Skills, Agents and workflows.</p>
          </div>
          </div>
        </li>
      </ol>
    </figure>

    <div class="architecture-privacy" data-reveal>
      <p>Public identity.<br /><span>Private intelligence.</span></p>
      <p>Service records are verifiable. Your prompts, code and runtime stay off-chain.</p>
    </div>
  </div>
</section>

<script>
  import "../scripts/architecture-cards";
</script>

<style>
  .architecture {
    width: min(1360px, calc(100% - 96px));
    min-height: calc(100svh - 64px);
    display: grid;
    align-items: center;
    padding-block: clamp(32px, 5svh, 64px);
    border-top: 0;
    scroll-margin-top: 64px;
  }

  .architecture-layout {
    display: grid;
    grid-template-columns: minmax(0, 0.86fr) minmax(0, 1.14fr);
    grid-template-areas: "intro diagram" "privacy diagram";
    gap: 48px 64px;
    align-items: center;
  }

  .architecture-intro { grid-area: intro; }
  .architecture-intro h2 span { display: block; }
  .architecture-intro h2 span:first-child { color: inherit; }
  .architecture-intro h2 span:not(:first-child) { color: var(--story-blue); }
  .architecture-intro > p { max-width: 400px; margin: 26px 0 0; color: var(--story-muted); font: 400 1rem/1.75 ui-sans-serif, system-ui, sans-serif; }

  .architecture-diagram { grid-area: diagram; margin: 0; min-width: 0; }
  .architecture-layers { list-style: none; margin: 0; padding: 0; }
  .architecture-layer { position: relative; display: grid; grid-template-columns: 56px minmax(0, 0.96fr) minmax(0, 1.04fr); gap: 22px; align-items: center; min-height: clamp(130px, 16.5svh, 170px); padding: 26px 22px; border: 1px solid #293b54; border-radius: 10px; background: #080f19; cursor: pointer; transform-origin: center; transition: background-color 200ms ease, border-color 200ms ease, box-shadow 200ms ease; }
  .architecture-layer[data-active="true"] { background: #142235; border-color: #6b9acf; box-shadow: 0 0 0 1px #5b8fe530; }
  .architecture-layer:focus-visible { outline: 2px solid var(--story-blue); outline-offset: 4px; }
  .architecture-layer::before { content: ""; position: absolute; left: 50px; top: 0; bottom: 0; width: 1px; background: #496e9c; }
  .architecture-layer--network { min-height: clamp(156px, 19svh, 188px); background: #080f19; border-color: #345982; border-radius: 10px; }
  .architecture-node { display: grid; place-items: center; width: 56px; height: 56px; border: 1px solid #42678f; border-radius: 50%; color: #c3d7ef; background: #03070d; font: 400 0.9rem/1 ui-monospace, monospace; position: relative; z-index: 1; }
  .architecture-node img { display: block; }
  .architecture-product > span { display: block; color: #96bdeb; font: 500 0.75rem/1.5 ui-monospace, monospace; letter-spacing: 0.03em; text-transform: uppercase; }
  .architecture-product h3 { margin: 8px 0 0; font-size: clamp(1.2rem, 2vw, 1.875rem); font-weight: 600; line-height: 1.2; letter-spacing: -0.035em; white-space: nowrap; }
  .architecture-layer > p { margin: 0; color: #a7b6ca; font: 400 1rem/1.65 ui-sans-serif, system-ui, sans-serif; }
  .architecture-layer > p > span { display: block; margin-top: 5px; color: var(--story-muted); }
  .architecture-connector { height: clamp(52px, 6.5svh, 66px); display: flex; align-items: center; gap: 23px; padding-left: 43px; color: #91b5e0; }
  .architecture-connector svg { flex-shrink: 0; width: 16px; height: 100%; }
  .architecture-connector > span { color: #98abc4; font: 400 0.75rem/1.5 ui-sans-serif, system-ui, sans-serif; }

  .architecture-privacy { grid-area: privacy; }
  .architecture-privacy::before { content: ""; display: block; width: 46px; height: 2px; background: var(--story-blue); margin-bottom: 22px; }
  .architecture-privacy > p:first-child { margin: 0; font-size: clamp(1.35rem, 2.2vw, 2rem); font-weight: 500; letter-spacing: -0.035em; line-height: 1.25; }
  .architecture-privacy > p:first-child span { color: var(--story-blue); }
  .architecture-privacy > p:last-child { max-width: 380px; margin: 14px 0 0; color: var(--story-muted); font: 400 0.875rem/1.7 ui-sans-serif, system-ui, sans-serif; }

  @media (min-width: 1024px) and (max-width: 1199px) {
    .architecture { width: calc(100% - 64px); }
    .architecture-layout { grid-template-columns: minmax(0, 0.78fr) minmax(0, 1.22fr); column-gap: 32px; }
    .architecture-layer { grid-template-columns: 38px minmax(0, 1fr) minmax(0, 1fr); gap: 14px; padding-inline: 16px; }
    .architecture-layer::before { left: 35px; }
    .architecture-node { width: 38px; height: 38px; }
    .architecture-node img { width: 26px; height: 26px; }
    .architecture-connector { padding-left: 28px; gap: 14px; }
    .architecture-layer > p { font-size: 0.875rem; }
  }

  @media (min-width: 1024px) and (max-height: 760px) {
    .architecture-layout { row-gap: 30px; }
    .architecture-intro > p { margin-top: 20px; }
    .architecture-layer { min-height: 112px; padding-block: 20px; }
    .architecture-layer--network { min-height: 136px; }
    .architecture-connector { height: 42px; }
    .architecture-connector svg { height: 36px; }
    .architecture-privacy::before { margin-bottom: 16px; }
  }

  @media (max-width: 1023px) {
    .architecture { width: min(640px, calc(100% - 40px)); min-height: 0; padding-block: 72px; }
    .architecture-layout { grid-template-columns: 1fr; grid-template-areas: "intro" "diagram" "privacy"; gap: 40px; }
    .architecture-intro > p { max-width: 520px; margin-top: 22px; }
    .architecture-privacy > p:last-child { max-width: 460px; }
  }

  @media (max-width: 540px) {
    .architecture-layer { grid-template-columns: 38px minmax(0, 1fr); gap: 0 18px; padding: 22px 18px; min-height: 0; }
    .architecture-layer::before { left: 37px; }
    .architecture-node { grid-row: 1 / 3; width: 38px; height: 38px; align-self: start; }
    .architecture-node img { width: 26px; height: 26px; }
    .architecture-product h3 { font-size: 1.45rem; margin-top: 5px; }
    .architecture-layer > p { grid-column: 2; font-size: 0.875rem; margin-top: 12px; }
    .architecture-layer > p > span { margin-top: 3px; }
    .architecture-connector { padding-left: 30px; gap: 20px; height: 42px; }
    .architecture-connector svg { height: 36px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .architecture-layer { transition: none; }
  }
</style>
